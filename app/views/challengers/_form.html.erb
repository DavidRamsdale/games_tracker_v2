<%= form_with(model: challenger, local: true) do |form| %>
  <% if challenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenger.errors.count, "error") %> prohibited this challenger from being saved:</h2>

      <ul>
      <% challenger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
